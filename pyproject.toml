[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "pyaicad"
version = "0.0.1"
requires-python= ">=3.8"
dependencies = [
    "cpmpy>=0.9.0",
]

[tool.maturin]
features = ["pyo3/extension-module"]
